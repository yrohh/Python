<에러 output 관련>

- 몇몇의 에러는 학습 과정에서 일부러 발생시킨 에러입니다.
- 그밖에 pandas.ix[] 와 관련해서는, 이제는 pandas에서 ix를 메소드를 지원하지 않아, 이를 포함하는 코드에서 에러가 발생합니다. 하지만 문제됨은 없습니다.
# 아래 참고.


Pandas의 버전이 2020년 1월 30일에 새롭게 업그레이드 되면서 그동안 삭제 예고 되었던 ix 메소드를 결국 삭제 하였습니다.

때문에 pandas 버전이 1.0.0 이상에서는 1장의 Pandas ix 예제 및 강의의 실습 코드중 ix가 사용된 코드는 제대로 동작하지 않을 것입니다.

2020년에 Anaconda로 Pandas를 셋업 하신 분들은 오류를 막기 위해서 아래 pip 명령어로 Pandas Downgrade를 요청 드립니다.  (conda install pandas==0.25.1는 imcompatible packages 를 찾느라 너무 시간이 오래 걸리는 군요)

pip install pandas==0.25.1

감사합니다.